package mainjava2;

public interface ImyDemo {
	
	public void printWebsite(String website);
	//if we add one more absract method, our lambda approach wont work
	//We would get error in main method if we do so //functional interface method
	
}